# =============================================================================
# MedVQA-PEFT 项目依赖文件 (requirements.txt)
# 版本：2026.04
# 项目：Qwen3.5-9B + Gemma-4-E4B-IT + LoRA/DoRA/PiSSA + VQA-RAD/SLAKE
# =============================================================================

# ==================== 【1. Unsloth 核心加速】（必须优先安装） ====================
# Unsloth 是本项目核心加速库，支持 Qwen3.5 和 Gemma-4 vision fine-tuning,安装后会自动包含最新 torch 兼容版本
unsloth
# ==================== 【2. PyTorch 基础环境】 ====================
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# ==================== 【3. Hugging Face 核心生态】 ====================
transformers>=4.48.0
peft>=0.14.0
trl>=0.11.0
accelerate>=0.35.0
datasets>=3.1.0
huggingface_hub>=0.26.0
bitsandbytes>=0.44.0          # 4-bit 量化必需
einops>=0.8.0

# ==================== 【4. 实验追踪与可视化】 ====================
wandb>=0.18.0
tqdm>=4.66.0
rich>=13.9.0
pandas>=2.2.0
numpy>=1.26.0

# ==================== 【5. 评估指标专用】 ====================
openai>=1.50.0                # GPT-4o-as-a-Judge 语义评分
scikit-learn>=1.5.0           # 用于计算 Precision/Recall/F1（可选）
Pillow>=10.4.0                # 图像处理

# ==================== 【6. 中文关键词提取（开放式问题 Keyword Recall 用）】 ====================
jieba>=0.42.1                 # 中文关键词提取（VQA-RAD/SLAKE 中文场景推荐）
scispacy>=0.5.5               # 可选：医学专业实体识别（英文场景更准）

# ==================== 【7. 可选加速与调试工具】 ====================
# flash-attn>=2.7.0            # 速度提升（可选，需 CUDA 12.1+）
# xformers>=0.0.28             # 内存优化（可选）